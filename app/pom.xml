<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.casehub</groupId>
    <artifactId>casehub-openclaw-parent</artifactId>
    <version>0.2-SNAPSHOT</version>
  </parent>

  <artifactId>casehub-openclaw-app</artifactId>

  <name>CaseHub OpenClaw :: App</name>
  <description>Quarkus deployment — OpenClaw delivery webhook endpoint (receives OpenClaw
    deliver:webhook results and dispatches to Qhorus), ChannelContextWindow REST service
    (GET /channel-context/{agentId}), OpenClaw-scoped named datasource</description>

  <dependencies>

    <!-- casehub-openclaw SPI implementations -->
    <dependency>
      <groupId>io.casehub</groupId>
      <artifactId>casehub-openclaw-casehub</artifactId>
    </dependency>

    <!-- casehub-platform MockPreferenceProvider @DefaultBean — must be runtime scope.
         Rationale: quarkus:build validates CDI at augmentation time without test classpath.
         runtime scope keeps MockPreferenceProvider visible at augmentation.
         See: PLATFORM.md §casehub-platform-api; protocol PP-20260524-a8f597. -->
    <dependency>
      <groupId>io.casehub</groupId>
      <artifactId>casehub-platform</artifactId>
      <scope>runtime</scope>
    </dependency>

    <!-- JQEvaluator required by casehub-engine CDI beans (engine#316) -->
    <dependency>
      <groupId>io.casehub</groupId>
      <artifactId>casehub-platform-expression</artifactId>
    </dependency>

    <!-- Quarkus MCP server (mcp4j) — exposes CaseHub commitment tools via MCP protocol -->
    <dependency>
      <groupId>io.quarkiverse.mcp</groupId>
      <artifactId>quarkus-mcp-server-http</artifactId>
      <version>1.11.1</version>
    </dependency>

    <!-- Quinoa — builds and bundles the Lit Web Components frontend -->
    <dependency>
      <groupId>io.quarkiverse.quinoa</groupId>
      <artifactId>quarkus-quinoa</artifactId>
      <version>2.8.3</version>
    </dependency>

    <!-- Quarkus application -->
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-rest</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-rest-jackson</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-scheduler</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-hibernate-orm-panache</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-flyway</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-jdbc-h2</artifactId>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-jdbc-postgresql</artifactId>
    </dependency>

    <!-- casehub-platform-oidc — activates OidcCurrentPrincipal @RequestScoped.
         Brings quarkus-oidc transitively. CDI wiring: see openclaw#41 spec §4.2. -->
    <dependency>
      <groupId>io.casehub</groupId>
      <artifactId>casehub-platform-oidc</artifactId>
    </dependency>

    <!-- Test -->
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-junit</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-test-security</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>io.quarkus</groupId>
      <artifactId>quarkus-junit-mockito</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>io.rest-assured</groupId>
      <artifactId>rest-assured</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>io.casehub</groupId>
      <artifactId>casehub-qhorus-testing</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>io.casehub</groupId>
      <artifactId>casehub-qhorus-persistence-memory</artifactId>
      <version>${project.version}</version>
      <scope>test</scope>
    </dependency>
    <!-- casehub-engine-testing is intentionally NOT included here.
         casehub-engine embeds an application.properties that self-indexes its beans —
         pulling in engine beans that require WorkerExecutionManager, JobScheduler,
         RoutingCursorStore, and JQEvaluator SPIs not provided in the test classpath.
         The ChannelContextWindowResource test uses @InjectMock for ChannelContextWindowService
         and has no dependency on engine infrastructure.
         See docs/BUGS-AND-ODDITIES.md if engine-testing is needed for future tests. -->
    <dependency>
      <groupId>io.casehub</groupId>
      <artifactId>casehub-platform-testing</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>io.casehub</groupId>
      <artifactId>casehub-pages-npm</artifactId>
      <version>0.2-SNAPSHOT</version>
    </dependency>
    <dependency>
      <groupId>io.casehub</groupId>
      <artifactId>casehub-blocks-ui-npm</artifactId>
      <version>0.1-SNAPSHOT</version>
    </dependency>

  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <executions>
          <execution>
            <id>unpack-casehub-packages</id>
            <phase>initialize</phase>
            <goals>
              <goal>unpack</goal>
            </goals>
            <configuration>
              <artifactItems>
                <artifactItem>
                  <groupId>io.casehub</groupId>
                  <artifactId>casehub-pages-npm</artifactId>
                  <version>0.2-SNAPSHOT</version>
                  <outputDirectory>${project.basedir}/src/main/webui/.casehub-packages</outputDirectory>
                </artifactItem>
                <artifactItem>
                  <groupId>io.casehub</groupId>
                  <artifactId>casehub-blocks-ui-npm</artifactId>
                  <version>0.1-SNAPSHOT</version>
                  <outputDirectory>${project.basedir}/src/main/webui/.casehub-packages</outputDirectory>
                </artifactItem>
              </artifactItems>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>io.quarkus.platform</groupId>
        <artifactId>quarkus-maven-plugin</artifactId>
        <version>${version.quarkus.platform}</version>
        <extensions>true</extensions>
        <executions>
          <execution>
            <goals>
              <goal>build</goal>
              <goal>generate-code</goal>
              <goal>generate-code-tests</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <annotationProcessorPaths>
            <path>
              <groupId>io.casehub</groupId>
              <artifactId>casehub-platform-graphql-generator</artifactId>
              <version>${casehub.version}</version>
            </path>
          </annotationProcessorPaths>
          <compilerArgs>
            <arg>-AdomainFilter=openclaw/commitments,openclaw/workitems,openclaw/scenarios,openclaw/context,openclaw/plugin</arg>
            <arg>-AgenerateGraphQL=false</arg>
          </compilerArgs>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile>
      <id>native</id>
      <activation>
        <property>
          <name>native</name>
        </property>
      </activation>
      <properties>
        <skipITs>false</skipITs>
        <quarkus.native.enabled>true</quarkus.native.enabled>
      </properties>
    </profile>
  </profiles>

</project>
